import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: 'red'
    },
    image: {
        height: 60,
        width: 60,
        borderRadius: 40,
        borderWidth: 1,
        borderColor: 'white'
    }
});

export default styles;