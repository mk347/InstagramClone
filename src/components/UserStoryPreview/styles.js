import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        
    },
    name: {
        textAlign: 'center',
        fontWeight: '500',
    },
});

export default styles;